## v3.2.0 - 2026-05-06
### Added
* Running `neo4j-cli` with no subcommand now launches an interactive REPL (enabled by default); the shell can be disabled with `--shell=false`, `CLI_SHELL_ENABLED=false`, or `shell.enabled: false` in the config file, and is automatically suppressed in `--agent` mode
* The Mixpanel analytics token can now be overridden via the `CLI_TELEMETRY_MIXPANEL_TOKEN` environment variable or `telemetry.mixpanel_token` in the config file, removing the need for a custom build to change it
* TOON (Token-Oriented Object Notation) is now available as an output format alongside `text`, `json`, `pretty-json`, `table`, and `graph`; select it per-call with `--format toon`, persist it via `cypher.output_format: toon` in the config file, and note that agent mode (`--agent`) now defaults to `toon` for token-efficient output when no format flag or config value is set
### Changed
* Cypher queries returning path values now produce structured JSON objects (`{"_type":"path","nodes":[...],"rels":[...]}`) instead of a lossy string summary; existing consumers of path output will need to update their parsing
### Fixed
* `admin show-users` and `admin show-databases` now correctly parse results from Neo4j; both commands were previously always returning an error due to unimplemented result parsing
* Invalid `log_level`, `log_format`, or `log_output` values in the config file now produce a clear error on startup instead of silently defaulting
