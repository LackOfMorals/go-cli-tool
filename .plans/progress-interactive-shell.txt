================================================================================
TASK-001: Fix shell package: update presentation.Service references
Date: 2026-05-05T00:00:00Z
================================================================================

Summary:
The shell package already used presentation.Service (the interface) correctly in
all required places. No code changes were needed — the acceptance criteria were
already satisfied by the existing implementation. This task was verified and
closed.

Files Changed:
- .plans/tasks-interactive-shell.yml (task status updated to completed)
- .plans/progress-interactive-shell.txt (created)

Key Decisions:
- The shell package was already correct: shell.go line 36 uses presentation.Service
  in the Shell interface, line 53 uses it for ShellContext.Presenter, and
  interactive.go lines 29/64 use it for the concrete field and setter.
- go build ./internal/shell/... and go test ./internal/shell/... both pass with
  no changes needed.

Next Task: task-002 (Change shell.enabled config default from false to true)

================================================================================
TASK-002: Change shell.enabled config default from false to true
Date: 2026-05-05T01:00:00Z
================================================================================

Summary:
Changed the viper default for shell.enabled from false to true in
internal/config/config.go. Added TestDefaults_ShellEnabled test to
config_test.go to assert the new default. All existing tests continue to pass
and golangci-lint reports zero issues.

Files Changed:
- internal/config/config.go (changed v.SetDefault("shell.enabled", false) to true on line 285)
- internal/config/config_test.go (added TestDefaults_ShellEnabled test)

Key Decisions:
- No existing test was asserting shell.enabled defaulted to false, so only an
  addition was needed (no test removal).
- Added a positive assertion test (TestDefaults_ShellEnabled) to lock in the
  new true default, following the pattern of TestDefaults_MetricsEnabled.

Next Task: task-003 (Create bridge adapter: BridgeCategory in internal/shell/bridge.go)

================================================================================
TASK-003: Create bridge adapter: BridgeCategory in internal/shell/bridge.go
Date: 2026-05-05T02:00:00Z
================================================================================

Summary:
Created internal/shell/bridge.go implementing BridgeCategory, which recursively
wraps a dispatch.Category tree as a shell.Category tree. Also added two
read-only accessor methods (Commands, Prerequisite) to dispatch.Category so the
bridge can read private fields without violating encapsulation.

Files Changed:
- internal/shell/bridge.go (created — BridgeCategory, renderResult, dispatchPrerequisite)
- internal/dispatch/category.go (added Commands() and Prerequisite() accessors)
- .plans/tasks-interactive-shell.yml (task-003 status updated to completed)

Key Decisions:
- dispatch.Category had no exported accessors for `commands` or `prerequisite`.
  Added Commands() (returns a copy of the map) and Prerequisite() rather than
  using reflection or moving bridge logic into the dispatch package, keeping the
  dependency direction correct (shell → dispatch, not dispatch → shell).
- renderResult checks result.FormatOverride and uses FormatAs when set, matching
  how the CLI dispatcher selects per-query formats (e.g. --format graph).
- dispatchPrerequisite is a thin named function (not inlined) for clarity and
  testability.
- The bridge only iterates CommandNames() (canonical names), not AllCommandNames(),
  to avoid registering alias keys as first-class commands — AddCommand handles
  aliases via the Aliases field.

Next Task: task-004 (Unit tests for bridge adapter)

================================================================================
TASK-004: Unit tests for bridge adapter
Date: 2026-05-05T03:00:00Z
================================================================================

Summary:
Created internal/shell/bridge_test.go with 11 test cases covering all acceptance
criteria and additional rendering paths. Uses a package-level stubPresenter
implementing presentation.Service and an identityCtxFor helper.

Files Changed:
- internal/shell/bridge_test.go (created)
- .plans/tasks-interactive-shell.yml (task-004 status updated to completed)

Key Decisions:
- Used package shell_test (external test package) to match the convention of all
  other shell package test files, relying only on exported API.
- stubPresenter captures lastData and lastFormat fields so tests can assert which
  presenter method was called with which data — avoids importing mock frameworks.
- Tests cover: MessageResult, Presentation via Format, FormatOverride via FormatAs,
  nil-presenter fallback to Message, handler error propagation, prerequisite failure
  blocking handler, prerequisite pass, no-prerequisite, recursive subcategory
  bridging, and ctxFor context forwarding.
- All 11 tests pass; golangci-lint reports 0 issues.

Next Task: task-005 (Add --shell flag to root command)

================================================================================
TASK-005: Add --shell flag to root command and wire to Overrides.ShellEnabled
Date: 2026-05-05T04:00:00Z
================================================================================

Summary:
Added a --shell persistent boolean flag to the root Cobra command in
cmd/neo4j-cli/app.go. The flag is wired to Overrides.ShellEnabled only when
it was explicitly set by the user (cobra's Changed() check), so the flag
default never silently clobbers config-file or env-var values.
CLI_SHELL_ENABLED env var continues to be handled automatically by viper's
AutomaticEnv (key remapping: shell.enabled → CLI_SHELL_ENABLED).

Files Changed:
- cmd/neo4j-cli/app.go (added shellFlag var, --shell persistent flag, Changed("shell") override)

Key Decisions:
- Flag default is `true` to match the viper default (task-002). This way
  `--shell` without a value is a no-op (same as the config default), and only
  `--shell=false` produces an override.
- Used the same Changed() guard pattern as `--no-metrics` and `--aura-timeout`
  so the flag default never overwrites config/env values.
- No new tests needed: config.applyOverrides already has coverage; the flag
  wiring is verified by the build + help output check.

Next Task: task-006 (Add root command RunE that launches the interactive shell)

================================================================================
TASK-006: Add root command RunE that launches the interactive shell
Date: 2026-05-05T05:00:00Z
================================================================================

Summary:
Added RunE to the root Cobra command that launches the interactive REPL when
no subcommand is given. Two functions were added to app.go: launchShell (the
RunE handler) and startShell (wiring logic). The shell import was added to
app.go. Agent mode or Shell.Enabled=false causes fallback to cmd.Help().

Files Changed:
- cmd/neo4j-cli/app.go (added shell import, RunE on root command, launchShell,
  startShell functions)

Key Decisions:
- launchShell checks agentMode first (returns cmd.Help()) so agent callers
  using the root command without a subcommand get help, not a REPL.
- startShell uses app.buildCategories() (which uses interactive prerequisites
  in non-agent mode) so missing credentials prompt inside the REPL.
- ctxFor closure in BridgeCategory sets AgentMode=false and AllowWrites=true
  since the interactive shell is always a human session — write operations
  should not be blocked.
- The root command Long description was updated to mention the interactive shell.
- golangci-lint reports 0 issues; go test ./... passes with all packages green.

Next Task: task-007 (Welcome banner for no-config first-time users)
