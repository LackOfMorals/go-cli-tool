================================================================================
TASK-001: Add internal/skill package: Agent type, AGENTS list, path helpers
Date: 2026-04-29T15:30:00Z
================================================================================

Summary:
Created internal/skill package with Agent struct, hardcoded 10-agent AGENTS slice
mirroring Rust reference, expandPath helper handling ~/$XDG_CONFIG_HOME, and
FindAgent/DetectAgents lookups. Tests use t.TempDir + HOME override.

Files Changed:
- internal/skill/agents.go (created — Agent struct, AGENTS slice, expandPath, FindAgent, DetectAgents)
- internal/skill/agents_test.go (created — TestExpandPath, TestExpandPathNoHome, TestFindAgent, TestAgentsCatalog, TestDetectAgents, TestAgentSkillsPath)

Key Decisions:
- Used os.LookupEnv for HOME so missing-HOME returns ok=false explicitly.
- strings.CutPrefix for ~/-prefix (Go 1.20+, available since project uses 1.24).
- DetectAgents returns []*Agent of pointers into AGENTS slice — matches Rust ergonomics.
- Test uses t.Setenv (auto-restored) and t.TempDir for HOME isolation.

Next Task: task-002

================================================================================
TASK-002: Add skill-additions.md stub and embed scaffolding
Date: 2026-04-29T15:45:00Z
================================================================================

Summary:
Added embed scaffolding for SKILL.md: skill-additions.md stub with the
agent-mode gotcha, embed.go exposing SkillMD []byte via //go:embed plus a
//go:generate directive targeting cmd/gen-skill, and a placeholder
skill.md.gen so the package builds before the generator exists.

Files Changed:
- internal/skill/skill-additions.md (created — single gotcha line)
- internal/skill/embed.go (created — //go:embed + //go:generate, exports SkillMD)
- internal/skill/skill.md.gen (created — placeholder; will be overwritten by generator)

Key Decisions:
- //go:generate path is `go run ../../cmd/gen-skill` (relative from internal/skill).
- Placeholder skill.md.gen mirrors final structure (heading + Gotchas) so
  embed sanity holds before generator lands.
- Used blank import `_ "embed"` per stdlib convention for embed-only files.

Next Task: task-003

================================================================================
TASK-003: Refactor cobra wiring so the command tree is buildable without wired services
Date: 2026-04-29T16:00:00Z
================================================================================

Summary:
Extracted the cobra tree construction from cmd/neo4j-cli into internal/cli
as BuildCobraTree(Options). Persistent flag bindings now live on a shared
*cli.Flags struct so cmd/neo4j-cli and cmd/gen-skill walk identical Use/
Short/Long/Flags/Example. RunE is injected via Options.RunFactory; passing
nil yields a tree with no service wiring (generator-friendly).

Files Changed:
- internal/cli/tree.go (created — Flags, Options, BuildCobraTree, subcommand builders)
- internal/cli/cli.go (deleted — placeholder dead-code file)
- cmd/neo4j-cli/app.go (modified — package-level flag vars collapsed to *cli.Flags; buildRootCommand delegates to cli.BuildCobraTree)

Key Decisions:
- Single Flags struct in internal/cli avoids splitting the source of truth
  between app and generator.
- RunFactory injection (not service injection) keeps the cli package free of
  service imports — important so cmd/gen-skill can import internal/cli without
  pulling repository, analytics, etc.
- runEFor returns nil when RunFactory is nil so cobra prints help instead of
  invoking a missing handler — exactly what the generator wants.
- All existing flag names, defaults, usage strings preserved verbatim; help
  output diff'd against pre-refactor and matches.

Next Task: task-004

================================================================================
TASK-004: Build cmd/gen-skill generator that walks cobra tree and writes skill.md.gen
Date: 2026-04-29T16:15:00Z
================================================================================

Summary:
Added cmd/gen-skill/main.go: builds the cobra tree via cli.BuildCobraTree
with nil services, walks subcommands in stable name order, and emits
deterministic Markdown to internal/skill/skill.md.gen. Output sections:
root heading + Short + Long, Global Flags table (root persistent flags),
per-subcommand `## <name>` with usage/short/long/flags/examples, then
`## Gotchas` appended from skill-additions.md.

Files Changed:
- cmd/gen-skill/main.go (created)
- internal/skill/skill.md.gen (regenerated from placeholder)

Key Decisions:
- projectRoot resolved via runtime.Caller so output is identical whether
  invoked via `go run ./cmd/gen-skill` (CWD=root) or `go generate ./...`
  (CWD=internal/skill).
- Persistent flags rendered once at root as Global Flags; subcommand flag
  tables show local flags only (avoids duplication when subcommands gain
  their own flags).
- Sorted both subcommands and flag rows by name for byte-stable output.
- Skipped Hidden cmds and the auto-injected `help` command.
- Pipe chars in flag usage strings are escaped for Markdown table cells.

Next Task: task-005

================================================================================
TASK-005: CI gate: fail PRs that forget to regenerate skill.md.gen
Date: 2026-04-29T17:30:00Z
================================================================================

Summary:
Added .github/workflows/ci.yml that installs mockgen, runs `go generate ./...`,
fails if any generated file (skill.md.gen, mocks) drifted, then runs build,
vet, and tests on push-to-main and pull_request.

Files Changed:
- .github/workflows/ci.yml (created)

Key Decisions:
- Created a new ci.yml rather than extending release.yml (release only fires
  on tag push — wrong trigger for a PR gate).
- Pinned mockgen to v0.6.0 to match go.sum (avoids version drift between the
  module dep and the binary).
- Diff entire worktree instead of only skill.md.gen — catches mock drift too,
  no extra cost.
- Single ubuntu runner; no matrix needed for this gate.

Next Task: task-006

================================================================================
TASK-006: Define SkillService interface and Filesystem abstraction
Date: 2026-04-29T17:50:00Z
================================================================================

Summary:
Added SkillService interface (Install/Remove/List) and result types
(InstallResult, RemoveResult, AgentStatus) to internal/service/interfaces.go
with JSON tags for agent-mode envelopes. Added Filesystem interface +
OSFilesystem wrapper in internal/skill/filesystem.go (Stat/Lstat/MkdirAll/
WriteFile/RemoveAll/Remove) so task-007's service is testable via a
TempDir-backed or fake fs without touching real $HOME.

Files Changed:
- internal/service/interfaces.go (modified — added Skill section)
- internal/skill/filesystem.go (created)

Key Decisions:
- Filesystem placed in internal/skill, not internal/service: skill is the
  only consumer and already owns the agent/embed domain. Keeps service
  package free of fs concerns.
- JSON tags use snake_case (display_name) — sets the convention for
  agent-mode envelopes; existing service result types lack JSON tags.
- Lstat included alongside Stat so task-007 can detect symlinked install
  targets that must be removed before WriteFile.
- OSFilesystem is a value receiver (zero-size struct) — no allocation cost,
  trivially comparable.

Next Task: task-007

================================================================================
TASK-007: Implement SkillService (Install / Remove / List)
Date: 2026-04-29T18:10:00Z
================================================================================

Summary:
Implemented SkillServiceImpl in internal/service/skill_service.go. Install
writes skill.SkillMD to <skills_dir>/neo4j-cli/SKILL.md per target agent;
empty agentName fans to all detected agents (errors if none); known but
undetected agent still installs. Remove deletes <skills_dir>/neo4j-cli/;
empty agentName removes from every installed agent. List returns AgentStatus
for all 10 catalog agents. No canonical/cache dir per PRD.

Files Changed:
- internal/service/skill_service.go (created — SkillServiceImpl + NewSkillService + NewSkillServiceWith)

Key Decisions:
- NewSkillServiceWith(fs, agents, content) constructor exposed so task-008
  tests can inject TempDir-backed Filesystem + alt agents/content without
  touching real $HOME or fiddling with HOME env.
- isInstalled uses Lstat (matches Rust symlink_metadata): a symlinked
  install still reports installed even if the link target is gone.
- isDetected uses Stat (follows symlinks): an agent dir that happens to be
  a symlink is still "installed".
- removePath wipes any prior file/dir/symlink before WriteFile so prior
  symlink-style installs (Rust era) don't collide with new file install.
- InstallResult.Path is the SKILL.md file path, not the dir — more useful
  for agent-mode JSON consumers.
- ctx is intentionally unused; kept on interface for future-proofing
  (cancellation if filesystem ops ever go remote).

Next Task: task-008

================================================================================
TASK-008: Service unit tests for SkillService with a TempDir-backed filesystem
Date: 2026-04-29T18:30:00Z
================================================================================

Summary:
Added internal/service/skill_service_test.go (11 tests) covering install/
remove/list across all PRD scenarios. Tests run against TempDir-isolated
$HOME with XDG_CONFIG_HOME unset, using OSFilesystem + a synthetic
3-agent catalog (alpha/beta/gamma) injected via NewSkillServiceWith so
production AGENTS catalog stays out of test paths.

Files Changed:
- internal/service/skill_service_test.go (created)

Key Decisions:
- Used real OSFilesystem against TempDir HOME instead of a fake fs —
  exercises expandPath/Stat/Lstat wiring end-to-end at no extra cost.
- Synthetic agents avoid coupling tests to changes in the production
  catalog (e.g. opencode's $XDG_CONFIG_HOME branch). All synthetic
  agents use ~/-prefix paths.
- Idempotency check baked into Install_OverExistingTarget rather than a
  separate test — same fixture, second invocation.
- Sort on Remove_AcrossMultipleAgents result names is defensive; the
  service emits catalog order today but the assertion stays correct
  if that ever changes.

Next Task: task-009

================================================================================
TASK-009: Build skill command category (install / remove / list)
Date: 2026-04-29T18:50:00Z
================================================================================

Summary:
Added internal/commands/skill.go with BuildSkillCategory(svc) exposing
install [agent] (ModeWrite), remove [agent] (ModeWrite), and list (ModeRead;
alias ls). Each handler calls SkillService and emits both presentation table
and items[] for table|json parity. No prerequisite — purely local fs ops.

Files Changed:
- internal/commands/skill.go (created)

Key Decisions:
- ListResult used for all three commands (even single-agent install) so the
  JSON envelope is uniform: array of {agent,path} / {agent} / status rows.
- boolYesNo helper renders Detected/Installed as yes/no in table mode while
  the JSON keeps native booleans — matches scannable status-column convention.
- Long/Example strings live on the cobra side per task spec; the dispatch
  Category only carries Usage + Description.
- list gets ls alias to match cloud instances list / config list ergonomics;
  install/remove deliberately have no aliases per spec.

Next Task: task-010

================================================================================
TASK-010: Wire skill subcommand into cmd/neo4j-cli/app.go
Date: 2026-04-29T19:10:00Z
================================================================================

Summary:
Wired SkillService into App and added the skill cobra subtree (parent +
install/remove/list leaves) to internal/cli/tree.go. Each leaf has its own
Short/Long/Example/Args; RunE delegates via runEForLeaf which prepends the
leaf name onto args before flowing through dispatchCategory("skill"). All
six --help and JSON acceptance probes pass; skill.md.gen regenerated and
contains a `## skill` section.

Files Changed:
- internal/cli/tree.go (added buildSkillCommand + runEForLeaf helper)
- cmd/neo4j-cli/app.go (added skillSvc field, NewSkillService wiring,
  skill entry in buildCategories)
- internal/skill/skill.md.gen (regenerated — adds `## skill` section)

Key Decisions:
- Leaf cobra commands instead of one flat skill command: required by
  acceptance criteria ("skill install --help shows usage/Long/flags/Example").
  cloud/cypher/admin/config get away with a single cobra node because their
  Long bodies summarise children; skill's leaves need first-class help.
- runEForLeaf added (not generalised to all categories) — only skill needs
  it today; keeping the helper narrow avoids changing existing dispatch flow
  for cypher's DisableFlagParsing trick.
- Args constraints (MaximumNArgs(1) on install/remove, NoArgs on list)
  surface argument errors at cobra parse time before the dispatcher runs.
- ls alias preserved on the list cobra leaf via Aliases:[]string{"ls"} —
  matches the dispatch.Category alias and keeps muscle memory consistent.
- Tested write probes against $(mktemp -d) HOME so the developer's real
  ~/.claude is never written to during verification.

Next Task: task-011

================================================================================
TASK-011: Command-layer tests for skill category using mockgen
Date: 2026-04-29T19:30:00Z
================================================================================

Summary:
Added mockgen directive in internal/service/interfaces.go (matches the
internal/analytics pattern) and committed the generated MockSkillService
under internal/service/mocks/. internal/commands/skill_test.go covers
all three commands (install/remove/list) with positional and no-positional
variants, error propagation, table vs json output, ls alias, agent-mode
write blocking via dispatcher (READ_ONLY AgentError), and read-mode pass-
through.

Files Changed:
- internal/service/interfaces.go (added //go:generate mockgen directive)
- internal/service/mocks/mock_skill_service.go (generated)
- internal/commands/skill_test.go (created — 15 tests)

Key Decisions:
- Used go.uber.org/mock (gomock) for SkillService since the analytics
  package already follows that pattern; the older hand-written mocks in
  cloud/admin/cypher tests are kept untouched to avoid scope creep.
- Agent-mode block tests intentionally set NO EXPECT() on the mock — if
  the dispatcher forwarded to the handler the gomock controller would
  fail the test, making the block guarantee a hard assertion.
- JSON-output coverage uses FormatAs(OutputFormatJSON) on the result's
  Presentation rather than re-serialising Items, exercising the same path
  app code takes when --format json is set.
- Reused humanOut helper from admin_test.go (same package: commands_test).
- Installed mockgen v0.6.0 via `go install go.uber.org/mock/mockgen@v0.6.0`
  to match go.sum; CI already does the same in ci.yml.

Next Task: task-012
