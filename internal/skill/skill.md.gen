# neo4j-cli

Placeholder SKILL.md. Run `go generate ./...` to regenerate from the cobra command tree.

## Gotchas

Always pass --agent when invoking neo4j-cli from another agent — output becomes JSON and writes are blocked unless --rw is also passed.
